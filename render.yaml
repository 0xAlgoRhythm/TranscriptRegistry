services:
  - type: web
    name: credaxis-mcp
    env: node
    rootDir: mcp
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false # You must paste your PostgreSQL connection string in the Render Dashboard
      - key: PORT
        value: 10000
